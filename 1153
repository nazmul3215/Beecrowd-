# Beecrowd-
#include<stdio.h>
int main()
{
    int N,i,c,p;
    scanf("%d", &N);

    for ( i=0 , p=1 ; N>i ; i++  )
    {
        c = (N - i);
        p *= c;

    }
    printf("%d\n", p);
    return 0;
}
